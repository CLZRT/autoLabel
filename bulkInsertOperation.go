package autolabel

func labelInstance_bulkInsert(logentry *AuditLogEntry) error {
	// Return a greeting that embeds the name in a message.
	//message := fmt.Sprintf("Hi, %v. Welcome!", name)
	return message
}
